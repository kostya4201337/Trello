spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        default_schema: TEST_KOSTYA
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:h2:mem:trello_test
    username: admin
    password: password
    driverClassName: org.h2.Driver
  flyway:
    schemas: TEST_KOSTYA

